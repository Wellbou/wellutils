#compdef wellhw
# zsh completion for wellhw
# Part of wellutils by wellbou_
# Generated by tools/gen-completions.sh from `wellhw --help` -- do not edit.

_wellhw() {
    _arguments -s \
        '--help[show this help]' \
        '-h[show this help]' \
        '--version[show version]' \
        '-V[show version]' \
        '--lang=[output language (auto = from locale)]:value:(ru en auto)' \
        '-l+[output language (auto = from locale)]:value:(ru en auto)' \
        '--color=[colorize output (auto = tty)]:value:(always auto never)' \
        '--plain[plain text, no box drawing]' \
        '--box[force box drawing even when piped]' \
        '--no-emoji[drop emoji icons]' \
        '--emoji[force emoji icons]' \
        '--json[machine-readable JSON]' \
        '--html[standalone HTML report page]' \
        '--debug[shell tracing]' \
        '--snapshot=-[save stable hardware identity to FILE]:file:_files' \
        '--diff=-[compare current hardware against a snapshot]:file:_files' \
        '*: :'
}

_wellhw "$@"
